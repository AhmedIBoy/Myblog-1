package com.myblog2.demo.myblog.Stream;

import java.lang.reflect.Array;
import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;

public class FilterQuestion {

    public static void main(String[] args) {
//        Example 2: Filtering and Mapping
//        Question: How would you filter a list of integers to get only even numbers and then square them?
//        List<Integer>  number = Arrays.asList(12,4,7,8,54,67,34);
//        List<Integer> evenSquares  = number.stream().filter(x -> x % 2 == 0).map(x -> x * x).collect(Collectors.toList());
//        System.out.println(evenSquares);


//        Example 4: Combining Filter with Other Operations
//        Question: How can you filter a list of strings to remove nulls and empty strings, and then count the remaining elements?

//        List<String> str = Arrays.asList("Ahmed","",null,"Mia","Kia","Fia");
//        long count = str.stream().filter(s -> s != null && !s.isEmpty()).count();
//        System.out.println(count);


//        Example 5: Filtering Based on Multiple Conditions
//        Question: How do you filter a list of numbers to get elements which are greater than 10 and even?

//        List<Integer> numbers = Arrays.asList(10,40,34,23,37,84,3,7);
//        List<Integer> data = numbers.stream().filter(n -> n > 10 && n % 2 == 0).collect(Collectors.toList());
//        System.out.println(data);


//        Example 2: Squaring Numbers in a List
//        Question: How do you use map() to square each number in a list of integers?

//        List<Integer> square = Arrays.asList(79,97,65,10,20,30);
//        List<Integer> dataSquare = square.stream().map(s -> s * s).collect(Collectors.toList());
//        System.out.println(dataSquare);


    }
}
