package com.myblog2.demo.myblog.Stream;

import java.util.Arrays;
import java.util.List;
import java.util.Locale;
import java.util.stream.Collectors;

public class Test {
    public static void main(String[] args) {
        //=======================================filter() Methods==================================================
//        List<Integer> Numbers = Arrays.asList(20, 38, 20, 40, 5, 2, 6);
//        List<String> names = Arrays.asList("Ahmed","Mike","Mohammed","Jabir");
//        List<Integer> data = Numbers.stream().filter(n -> n % 2 == 0).collect(Collectors.toList());
//        List<Integer> data1 = Numbers.stream().filter(n -> n % 2 != 0).collect(Collectors.toList());
//        List<String> data2 = names.stream().filter(s -> s.startsWith("A")).collect(Collectors.toList());
//        List<String> data3 = names.stream().filter(s -> s.endsWith("e")).collect(Collectors.toList());
//        List<String> data4 = names.stream().filter(s -> s.equals("Mohammed")).collect(Collectors.toList());
//        System.out.println(data);
//        System.out.println(data1);
//        System.out.println(data2);
//        System.out.println(data3);
//        System.out.println(data4);


//=======================================Map() Methods=================================================
//=====Map() is accept internally function functional interface function take input and processed an a result ===========
//        List<Integer> numbers = Arrays.asList(10,7,2,5,2,7,33,21);
//        List<String> names = Arrays.asList("Ahmed","Mohammed","Jabir","Amir");
//        List<Integer> data = numbers.stream().map(i -> i + 10).collect(Collectors.toList());
//        List<Integer> data2 = numbers.stream().map(i -> i * 10).collect(Collectors.toList());
//        List<String> data3 = names.stream().map(s -> s.toUpperCase()).collect(Collectors.toList());
//        List<String> data4 = names.stream().sorted().map(s -> s.toUpperCase()).collect(Collectors.toList());
//        List<String> data5 = names.stream().distinct().map(s -> s.toLowerCase()).collect(Collectors.toList());
//        System.out.println(data2);
//        System.out.println(data);
//        System.out.println(data3);
//        System.out.println(data4);
//        System.out.println(data5);
    }
}
